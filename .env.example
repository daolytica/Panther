# Example environment variables (copy to .env.local if needed)
# DO NOT commit .env or .env.local - they may contain secrets

# Browser mode: API server URL (optional, defaults to http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# HTTP server port for browser mode (set when running panther-http-server)
# PANTHER_HTTP_PORT=3001
